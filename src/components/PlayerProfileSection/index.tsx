import { PlayerProfileRoot } from './PlayerProfileRoot'
import { PlayerProfileTitle } from './PlayerProfileTitle'
import { PlayerProfileGraphLegend } from './PlayerProfileGraphLegend'
import { PlayerProfileGraph } from './PlayerProfileGraph'

export const PlayerProfile = {
  Root: PlayerProfileRoot,
  Title: PlayerProfileTitle,
  Graph: PlayerProfileGraph,
  Legend: PlayerProfileGraphLegend,
}
