import { FormPlayerProfileType } from '../types/palyerProfileTypes'

export function getPlayerGameStylesData() {
  return (values: FormPlayerProfileType) => {
    // eslint-disable-next-line no-unused-expressions
    values
  }
}
